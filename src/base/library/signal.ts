import { signal as NewSignal, computed as NewComputedSignal, effect as NewEffect, batch as NewBatch, untracked as NewUntracked } from "@preact/signals-core";

export { NewSignal, NewComputedSignal, NewEffect, NewBatch, NewUntracked };

export { NewSignal as nsi, NewComputedSignal as ncs, NewEffect as nef, NewBatch as nbt, NewUntracked as nut };

export { Signal, ReadonlySignal } from "@preact/signals-core";

export type * from "@preact/signals-core";
